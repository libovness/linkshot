<%= form.fields_for :links, remote: true do |link_form| %>
	<div class="field shot-link-field">  		
		<%= link_form.label :title, 'Link name' %>
		<%= link_form.text_field :title %>
		<%= link_form.label :url, 'Link url' %>
		<%= link_form.text_field :url %>
		<%#= link_to "Remove", @link, method: :delete, data: {confirm: "Are you sure?"}, remote: true %>
	</div>
<% end %>
<%= link_to "New Link", new_link_path, id: "new_link_link", remote: true %>